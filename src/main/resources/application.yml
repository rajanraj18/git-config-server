  
spring:
  application:
    name: git-config-server
 
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rajanraj18/configuration-server
          default-label: main
          search-paths:
          - '*service*'
          - '*station*'


    
